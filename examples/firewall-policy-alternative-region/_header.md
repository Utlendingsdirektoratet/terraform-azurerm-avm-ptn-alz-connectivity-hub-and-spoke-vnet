# Firewall Policy in Alternative Region

Demonstrates how to create an Azure Firewall Policy in a different region to the Azure Firewall instance itself. This is useful in scenarios where you want to leverage a global base policy that resides in a specific region.
